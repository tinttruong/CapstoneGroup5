let found: boolean = true;
let grade: number = 88.6;         // integer or decimal
let firstName: string = "Tristan";   // double or single quotes
let lastName: string = "Anderson";

console.log(found);
console.log(`The grade is ${grade}`); // Back ticks instead of quote allow use of ${}
console.log(`This grade was acheived by ${firstName} ${lastName}`); // Known as template strings
